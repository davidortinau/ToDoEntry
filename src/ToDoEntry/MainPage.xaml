<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
    xmlns:common="clr-namespace:Common.Styles"
    x:Class="ToDoEntry.MainPage"
    xmlns:controls="clr-namespace:ToDoEntry.Controls">

    <!--<VisualStateManager.VisualStateGroups>
        <VisualStateGroup
            x:Name="CommonStates">
            <VisualState
                x:Name="Normal">
                <VisualState.Setters>
                    <Setter
                        TargetName="Favorite"
                        Property="Label.TextColor"
                        Value="Red" />

                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateManager.VisualStateGroups>-->

    <ContentPage.Resources>
        
    </ContentPage.Resources>

    <StackLayout
        Padding="15,100,15,0">

        <controls:TaskTitleView Title="{Binding Title}"
                                IsCompleted="False"
                                IsFavorite="False"/>

    </StackLayout>

</ContentPage>
